<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

    <mapper namespace="org.zerock.myapp.mapper.MyPlanMapper">
    <insert id="make">
        <selectKey keyProperty="planNo" order="AFTER" resultType="int">
            SELECT "ISEQ$$_74701".CURRVAL FROM dual  
        </selectKey>
            INSERT INTO TBL_MYPLAN (USERNO, PLANNAME, STARTDATE, ENDDATE) VALUES (#{userNo}, #{planName}, #{startDate}, #{endDate})
    </insert>

    </mapper>